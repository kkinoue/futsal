<nav class="navbar navbar-default navbar-fixed-top navbar-inverse"><%# page上部固定header %>
  <div class="container-fluid"><%# page上部固定headerのcontent %>
    <%= link_to 'Futsal', root_path, id: 'logo' %>

    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'ホーム', root_path %></li>
        <% if signed_in? %>
            <% if current_user.admin? %>
                <li><%= link_to 'イベント登録', new_event_url %></li>
            <% end %>
            <li><%= link_to 'イベント', events_path %></li>
            <% if current_user.admin? %>
              <li><%= link_to 'ユーザー一覧', users_path %></li>
            <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
               aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %>
              <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to '編集', edit_user_path(current_user) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'サインアウト', signout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'ユーザー登録', signup_path %></li>
          <li><%= link_to 'サインイン', signin_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>