<nav class="navbar navbar-default navbar-fixed-top navbar-inverse"><%# page上部固定header %>
  <div class="container"><%# page上部固定headerのcontent %>
    <%= link_to 'Futsal', root_path, id: 'logo' %>

    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Home', root_path %></li>
        <li><%= link_to 'Sigh up', signup_path %></li>
        <% if signed_in? %>
            <% if current_user.admin? %>
                <li><%= link_to 'Event new', new_event_url %></li>
            <% end %>
            <li><%= link_to 'Event', events_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
               aria-haspopup="true" aria-expanded="false">Account <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">Edit</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Sigh out', signout_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Sigh in', signin_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
